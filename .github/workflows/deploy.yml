name: Deploy to ConoHa VPS

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up SSH key
        uses: webfactory/ssh-agent@v0.5.5
        with:
          ssh-private-key: ${{secrets.STAGING_SSH_PRIVATE_KEY}}

      - name: Deploy to ConoHa VPS
        run: |
          ssh koichi@118.27.68.201 'cd /home/koichi/tennis_log_api && git pull origin develop
